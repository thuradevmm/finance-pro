import { ComingSoonPage } from "@/components/app/coming-soon-page";

export default function SettingsPage() {
  return (
    <ComingSoonPage
      activeNavLabel="Settings"
      description="Profile, currency, date format, and system preferences will be available here."
      icon="settings"
      title="Settings"
    />
  );
}
